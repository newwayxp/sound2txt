# Current Task

> Update this file before each session limit approaches.  
> Check `git log --oneline -10` and recent ARCHITECTURE.md sections for context.

---

## Project Status

**Version:** v1.3.12 (stable as of 2026-06-04)  
**Branch:** master  
**Known issues:** None — stable operation confirmed.

---

## Recently Completed (last 10 commits)

| Commit | Summary |
|--------|---------|
| `db628bd` | Delete wrong file |
| `b6d1d9a` | Add Claude performance reference file |
| `d7e0724` | README corrections |
| `be202e5` | v1.3.12: Fix installer + transcriber cleanup |
| `4e5f617` | Remove vocabulary.txt from git tracking |
| `7646cb6` | Exclude run.bat from git (generated by setup.bat) |
| `6b183b3` | README + setup.bat: proxy and vocabulary path docs |
| `bd65ab5` | Remove ui.py (customtkinter) — ui_qt.py is active UI |
| `6617a57` | Remove Mic enable toggle from Recording tab |
| `1bb1e71` | Move vocabulary.txt to user data folder (corrected_dir) |

---

## Active Work

None currently. Project is at a stable checkpoint.

---

## Pending / Next Steps

*(Fill in when a task is picked up)*

- [ ] ...

---

## How to Update This File

Before a session ends (approaching context limit):

1. Run `git log --oneline -10` to capture recent commits.
2. Check which files changed in those commits.
3. Read only those specific files (not the whole repo).
4. Update **Recently Completed**, **Active Work**, and **Pending** sections above.
5. If the changes affect architecture (new module, new signal file, UI redesign), also update `ARCHITECTURE.md` for the relevant section only.
