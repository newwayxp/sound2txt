# Sound2Text v1.3.14 リリースノート

**リリース日**: 2026-06-06
**前回リリース**: v1.3.11

---

## 🎙 マイク録音対応

- システム音声（相手の声）と同時にローカルマイク（自分の声）を収録
- 転写テキストに発話元ラベルを自動付与
  - 日本語: `【自分】`
  - 中国語: `【自己】`
  - 英語: `[Me]`
- On Air ボタンでマイク録音の ON/OFF を制御（PTT）
- セッション終了後の MP3 に、マイク音声をミリ秒精度で時系列合成（ffmpeg `adelay` + `amix`）

---

## ⚡ リアルタイム転写

- 録音停止を待たず、発話の無音区間ごとに即時転写（VAD セグメント単位）
- 転写完了ごとに LLM でノイズ補正（per-segment correction）
- Transcript タブにリアルタイム表示

---

## 🔇 エコー除去（AEC）

- スピーカーから出た音がマイクに回り込んで二重転写される問題を抑制
- scipy 相互相関によるセグメントレベルエコー検出
- 感度を `config.ini` の `aec_threshold` で調整可能（デフォルト `0.55`）
  - 値を小さくする → より積極的にエコー除去
  - 値を大きくする → 誤抑制を減らす

---

## 🌐 多言語対応の強化

- LLM 補正プロンプトを言語別に切り替え（zh / ja / en）
- セッション開始時に `config.ini` から言語設定を再読み込み（セッション間で言語変更が反映される）
- `[summary]` セクションに `enable_correction = false` を追加すると LLM 補正を完全無効化

---

## 🖥 UI 改善

- **タブ追加**: Transcript（生転写）/ Minutes（会議メモ）タブ
- **VU メーター刷新**: 白→青→黄→赤のゾーンブロック＋3層発光エフェクト（外側ヘイズ・内側ブルーム・中心輝線）
- **ダッシュボードタイマー**: パイプラインのログをリアルタイムで反映
- **言語選択コンボ**: 幅を拡大し全選択肢が表示されるように修正
- 転写時間タイマーの二重計上バグを修正（マイク音声が elapsed を超える問題）

---

## ⏱ 遅延改善

| パラメータ | 変更前 | 変更後 | 効果 |
|---|---|---|---|
| VAD `max_sec` | 30s | **12s** | 最初の転写が出るまでの時間を約60秒→20秒に短縮 |
| VAD `silence_sec` | 2.0s | **1.5s** | 短い無音でも文末を検出 |

---

## 🐛 主なバグ修正

| 修正内容 | 症状 |
|---|---|
| セッション再開時に前回の会議メモが残る | 停止→開始で古い内容が表示されていた |
| 転写ゼロ問題 | `temperature=0.0` で無音判定が多発 |
| VAD ノイズスキップ | 短いノイズでバッファがリセットされていた |
| Whisper ハルシネーション | `initial_prompt` が転写テキストに漏れる |
| CUDA/CPU フォールバック | GPU エラー時に CPU で自動再実行 |

---

## ⚙ 設定ファイル変更

### 削除された項目

| セクション | キー | 理由 |
|---|---|---|
| `[paths]` | `mic_dir` | コード内で未使用 |
| `[recording]` | `record_sec` | コード内で未使用 |
| `[subtitle]` | `dst_lang`, `model_size`, `src_lang`, `vad_threshold` | 字幕機能の廃止に伴い不要 |

### 追加された項目

```ini
[recording]
aec_threshold = 0.55    # エコー除去感度（0.0〜1.0）

[subtitle]
max_sec       = 12.0    # VAD 最大蓄積時間（秒）
silence_sec   = 1.5     # VAD 無音判定時間（秒）
min_accum_sec = 1.0     # VAD 最低蓄積時間（秒）

[summary]
enable_correction = false   # LLM補正を無効化したい場合に追加
```

---

## アップグレード手順

既存インストール済みの場合は `config.ini` の不要キーを手動で削除してください。

```ini
# 削除: [paths] の mic_dir 行
# 削除: [recording] の record_sec 行
# 追加: [recording] の aec_threshold = 0.55
# 追加: [subtitle] の max_sec = 12.0 と silence_sec = 1.5
```

新規インストールの場合は `setup.bat` を実行するだけで設定が自動作成されます。
