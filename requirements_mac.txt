# macOS dependencies
# System requirements (install via Homebrew before running pip):
#   brew install portaudio ffmpeg blackhole-2ch
#
# BlackHole provides system-audio loopback capture.
# PortAudio is the backend for pyaudio.

faster-whisper>=1.0.0
PyQt6>=6.4.0
transformers>=4.35.0
requests>=2.28.0
numpy>=1.24.0
pyaudio>=0.2.14
scipy>=1.10.0
